var fs = require("fs");
var wstream = fs.createWriteStream("./log/log.txt"); //var path = "./log/log.txt";
var data;

var writelog = function(data) {
  var date = new Date();
  var buff = date + " " + data + "\n";
  //wstream.write(buff, 0, buff.length, 0, function(error) {   //  fs.appendFile(path, date + data + "\n", function(error) {
	//	if (error) {
	//		console.error("*** writelog error : " + error.message);
	//	} else {
			//console.log("Successful Write to " + path);
	//	}
	//})
};

exports.writelog = writelog;
